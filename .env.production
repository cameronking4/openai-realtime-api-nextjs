# Production Environment Variables
# These values are used in production but don't contain secrets
# Actual secrets should be set in Vercel's environment variables UI

# Application Settings
NODE_ENV=production
NEXT_PUBLIC_API_URL=https://your-production-domain.com/api

# Note: In production, database credentials and API keys should be set
# through Vercel's environment variables UI, not in this file 